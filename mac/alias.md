# Goproxy
```
alias biligoproxy='export GOPROXY=http://goproxy.bilibili.co'
alias iogoproxy='export GOPROXY=https://goproxy.io'
alias ungoproxy='unset GOPROXY'
```

# Git Hook
```
alias enable_pre_commit='chmod a+x `pwd`/.git/hooks/pre-commit'
alias disable_pre_commit='chmod a-x `pwd`/.git/hooks/pre-commit'
```

# Command
```
alias ll='ls -alF'
alias la='ls -A'
alias l='ls -CF'
```